use std::str::pattern::Searcher;


fn main() {
    // 字符串匹配模式的原理
    // 字符串匹配背后其实是一套统一的迭代器适配器

    Searcher


}

// fn matches<'a, P: Pattern<'a>>(s: &'a str, pat: P) -> Matches<'a, P> {
//     // Matches(Matches)
// }
